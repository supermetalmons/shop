The decoder files in `0.185.1/` are copied unchanged from:

`three@0.185.1/examples/jsm/libs/draco/gltf/`

They are Draco decoder builds licensed under the Apache License 2.0. See
`LICENSE` for the license terms and `AUTHORS` for the upstream attribution.

Keep the versioned directory and `three` dependency in sync when upgrading Three.js.
